--- a/resources/scripts/components/server/files/FileManagerContainer.tsx
+++ b/resources/scripts/components/server/files/FileManagerContainer.tsx
@@ -76,6 +76,8 @@
                     <Can action={'file.create'}>
                         <div className={style.manager_actions}>
                             <FileManagerStatus />
+                            {/* [Notur] File manager toolbar extension slot */}
+                            <div id="notur-slot-server.files.actions" style={{ display: 'contents' }}></div>
                             <NewDirectoryButton />
                             <UploadButton />
                             <NavLink to={`/server/${id}/files/new${window.location.hash}`}>
@@ -85,6 +87,7 @@
                     </Can>
                 </div>
             </ErrorBoundary>
+            <div id="notur-slot-server.files.header" style={{ display: 'contents' }}></div>
             {!files ? (
                 <Spinner size={'large'} centered />
             ) : (
@@ -111,6 +114,7 @@
                     )}
                 </>
             )}
+            <div id="notur-slot-server.files.footer" style={{ display: 'contents' }}></div>
         </ServerContentBlock>
     );
 };
