--- a/resources/scripts/components/server/console/ServerConsoleContainer.tsx	2026-02-02 00:00:00
+++ b/resources/scripts/components/server/console/ServerConsoleContainer.tsx	2026-02-02 00:00:00
@@ -45,6 +45,8 @@
                     <Can action={['control.start', 'control.stop', 'control.restart']} matchAny>
                         <PowerButtons className={'flex sm:justify-end space-x-2'} />
                     </Can>
+                    {/* [Notur] Terminal power buttons extension slot */}
+                    <div id="notur-slot-server.terminal.buttons" style={{ display: 'contents' }}></div>
                 </div>
             </div>
             <div className={'grid grid-cols-4 gap-2 sm:gap-4 mb-4'}>
@@ -29,6 +29,7 @@
             {(isNodeUnderMaintenance || isInstalling || isTransferring) && (
                 <Alert type={'warning'} className={'mb-4'}>
                     {isNodeUnderMaintenance
                         ? 'The node of this server is currently under maintenance and all actions are unavailable.'
                         : isInstalling
                         ? 'This server is currently running its installation process and most actions are unavailable.'
                         : 'This server is currently being transferred to another node and all actions are unavailable.'}
                 </Alert>
             )}
+            <div id="notur-slot-server.console.header" style={{ display: 'contents' }}></div>
             <div className={'grid grid-cols-4 gap-4 mb-4'}>
@@ -58,7 +59,11 @@
             <div className={'grid grid-cols-4 gap-2 sm:gap-4 mb-4'}>
                 <div className={'flex col-span-4 lg:col-span-3'}>
                     <Spinner.Suspense>
                         <Console />
                     </Spinner.Suspense>
                 </div>
-                <ServerDetailsBlock className={'col-span-4 lg:col-span-1 order-last lg:order-none'} />
+                <div className={'col-span-4 lg:col-span-1 order-last lg:order-none flex flex-col gap-4'}>
+                    <ServerDetailsBlock />
+                    <div id="notur-slot-server.console.sidebar" style={{ display: 'contents' }}></div>
+                </div>
             </div>
             <div className={'grid grid-cols-1 md:grid-cols-3 gap-2 sm:gap-4'}>
                 <Spinner.Suspense>
                     <StatGraphs />
                 </Spinner.Suspense>
             </div>
+            <div id="notur-slot-server.console.footer" style={{ display: 'contents' }}></div>
             <Features enabled={eggFeatures} />
